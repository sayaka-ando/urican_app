<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<title>初心者向けホームページテンプレート tp_beginner5</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="description" content="ここにサイト説明を入れます">
<meta name="keywords" content="キーワード１,キーワード２,キーワード３,キーワード４,キーワード５">
<link rel="stylesheet" href="css/style.css">
<!--[if lt IE 9]>
<script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
<script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
<![endif]-->
</head>

<body>

<div id="container">

  <header>
    <div>
      <%= link_to 'urican_app', root_path, class:"menubar" %>
    </div>

    <nav id="menubar">
      <ul>
        <% if user_signed_in? %>
          <li><%= link_to current_user.name, "#", class: "user-nickname" %></li>
          <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class:"logout" %></li>
        <% else %>
          <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
          <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
        <% end %>
      </ul>
    </nav>
  </header>

<%# プラン概要 %>
  <div id="contents">
    <h2>提案プラン</h2>
      <table class="detail-table">
        <tbody>
          
          <tr>
            <th class="detail-item">提案プラン</th>
            <td class="detail-value"><%= @plan.title %></td>
          </tr>
          <tr>
            <th class="detail-item">顧客名</th>
            <td class="detail-value"><%= @plan.client %></td>
          </tr>
          <tr>
            <th class="detail-item">顧客ステータス</th>
            <td class="detail-value"><%= @plan.client_status.name %></td>
          </tr>
          <tr>
            <th class="detail-item">金額</th>
            <td class="detail-value"><%= @plan.price %>円</td>
          </tr>
          <tr>
            <th class="detail-item">受注ステータス</th>
            <td class="detail-value"><%= @plan.sales_status.name  %></td>
          </tr>
          <tr>
            <th class="detail-item">担当</th>
            <td class="detail-value"><%= @plan.user.name %></td>
          </tr>
          <tr>
            <th class="detail-item">メモ</th>
            <td class="detail-value"><%= @plan.memo %></td>
          </tr>
          <tr>
            <th class="detail-item">ナレッジ</th>
            <td class="detail-value"><%= @plan.knowledge %></td>
          </tr>
        </tbody>
      </table>
      <% if user_signed_in?%>
        <% if current_user.id == @plan.user_id %>
          <%= link_to '編集', edit_plan_path(@plan.id), class: "item-red-btn" %>
          <%= link_to '削除', plan_path(@plan.id), method: :delete, class:'item-destroy' %>
        <% end %>
      <% end %>
  </div>
</div>

<footer>
<small>Copyright&copy; <a href="index.html">SAMPLE SITE</a> All Rights Reserved.</small>
<span class="pr">《<a href="https://template-party.com/" target="_blank">Web Design:Template-Party</a>》</span>
</footer>

</div>
<!--/container-->

</body>
</html>

